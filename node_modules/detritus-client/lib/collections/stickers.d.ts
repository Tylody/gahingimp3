import { BaseClientCollectionOptions, BaseClientGuildReferenceCache } from './basecollection';
import { Sticker } from '../structures/sticker';
/**
 * @category Collection Options
 */
export interface StickersOptions extends BaseClientCollectionOptions {
}
/**
 * Stickers Reference Collection
 * @category Collections
 */
export declare class Stickers extends BaseClientGuildReferenceCache<string, Sticker> {
    key: string;
    insert(sticker: Sticker): void;
    get [Symbol.toStringTag](): string;
}
